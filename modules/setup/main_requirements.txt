# Core dependencies - REQUIRED
# Note: Python 3.12+ recommended (matches environment.yml)
torch>=2.0.0
torchvision>=0.12.0
numpy>=1.20,<2.0
scipy>=1.9
pyyaml>=6.0
termcolor>=2.3.0
psutil>=5.9.5
sortedcontainers>=2.4
pandas>=2.0
packaging>=20.0
tqdm>=4.64

# ONNX ecosystem - REQUIRED
onnx
git+https://github.com/doctormeee/onnx2pytorch.git
onnxruntime>=1.15
onnxoptimizer>=0.3
skl2onnx>=1.14

# Solvers - REQUIRED
gurobipy>=10

# Development tools - REQUIRED
pylint>=2.15
appdirs>=1.4
graphviz>=0.20.3
ninja>=1.10,<1.11.1.2

# Jupyter widgets - REQUIRED for interactive trace explorer
ipywidgets>=8.0.0

# Model loading (for Keras/TF models) - REQUIRED
tensorflow>=2.13.0

# ML frameworks (for pipeline/llm_probe.py) - REQUIRED
transformers>=4.30.0
scikit-learn>=1.0.0

# Note: The following are transitive dependencies (auto-installed):
# - requests, pillow (via torch/torchvision)
# - protobuf, typing-extensions (via onnx/tensorflow)
# - All nvidia-* packages (via torch with CUDA)
# - sympy, networkx (via torch)
# - certifi, charset-normalizer, urllib3, idna (via requests)
# - markupsafe, jinja2 (via tensorflow)
# - Other utility packages
